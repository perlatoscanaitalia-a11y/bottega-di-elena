---
interface Props { title: string; items: string[]; intro?: string; }
const { title, items, intro } = Astro.props;
---
<section class="product-group"><h2>{title}</h2>{intro && <p>{intro}</p>}<ul class="product-list">{items.map((item) => <li>{item}</li>)}</ul></section>

